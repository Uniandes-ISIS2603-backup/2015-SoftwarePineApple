(function (angular) {
    var countryModule = angular.module('saleModule');

    countryModule.run(['$httpBackend', 'sale.context', 'MockModule.mockRecords', 'sale.skipMock', 'MockModule.baseUrl', function ($httpBackend, context, mockRecords, skipMock, baseUrl) {
            
            
        }]);
})(window.angular);